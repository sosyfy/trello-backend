// Import the PrismaClient class from the "@prisma/client" package
import { PrismaClient } from "@prisma/client";

// Create a new instance of the PrismaClient class and export it
export const prisma = new PrismaClient();

